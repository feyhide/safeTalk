in messagebox 
 - friends
    .leave chat (add user to old members 
      allow other user to add the userback)
    .if chat has no members (all members leave then delete chat)
    .block
 - groups
    .(change groupName)
    .admin , admin add or remove members
    .leave group
    .admin delete group
    .if group has no members then delete group
   
speed check on msg sending

file and images

groupname regex
username regex
